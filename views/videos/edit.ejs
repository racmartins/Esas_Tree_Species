<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Vídeo</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5;
        padding-top: 50px;
      }
      .container {
        max-width: 800px;
        margin: auto;
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      }
      .button-group {
        display: flex;
        justify-content: center; /* Centra os botões */
        gap: 1rem; /* Espaçamento entre os botões */
      }
      .btn-primary {
        margin-top: 1rem;
        background-color: #198754; /* Cor verde para botão atualizar */
        color: white;
      }
      .btn-primary:hover {
        background-color: #218838;
        border-color: #1e7e34;
      }
      .btn-outline-secondary {
        margin-top: 1rem;
        background-color: #6c757d; /* Cor cinza para botão cancelar */
        color: white;
      }
      .btn-outline-secondary:hover {
        background-color: #28a745;
        color: #fff;
      }
      .icon {
        color: #28a745;
        margin-right: 0.5rem;
      }
      h1 {
        color: #28a745;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center">
        <i class="fa-solid fa-video icon"></i> Editar Vídeo
      </h1>
      <form action="/videos/<%= video._id %>?_method=PUT" method="POST">
        <div class="mb-3">
          <label for="title" class="form-label">Título do Vídeo</label>
          <input
            type="text"
            class="form-control"
            id="title"
            name="title"
            value="<%= video.title %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="description" class="form-label">Descrição</label>
          <textarea
            class="form-control"
            id="description"
            name="description"
            rows="3"
            required
          >
<%= video.description.trim() %></textarea
          >
        </div>
        <div class="mb-3">
          <label for="video_url" class="form-label">URL do Vídeo</label>
          <input
            type="url"
            class="form-control"
            id="video_url"
            name="video_url"
            value="<%= video.video_url %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="thumbnail_url" class="form-label">URL do Thumbnail</label>
          <input
            type="url"
            class="form-control"
            id="thumbnail_url"
            name="thumbnail_url"
            value="<%= video.thumbnail_url %>"
            required
          />
        </div>
        <div class="button-group">
          <button type="submit" class="btn btn-primary">
            <i class="fa-solid fa-check icon"></i> Atualizar Vídeo
          </button>
          <button
            type="button"
            class="btn btn-outline-secondary"
            onclick="window.history.back();"
          >
            <i class="fa-solid fa-arrow-left icon"></i> Cancelar
          </button>
        </div>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
