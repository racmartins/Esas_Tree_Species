<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Detalhes da Espécie</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="container py-4">
    <h1>Detalhes da Espécie: <%= species.common_name %></h1>
    <div class="card">
      <img
        src="<%= species.image_url ? species.image_url : '/img/img-padrao.png' %>"
        class="card-img-top"
        alt="<%= species.common_name %>"
      />

      <div class="card-body">
        <h3 class="card-title"><%= species.scientific_name %></h3>
        <p class="card-text"><%= species.description %></p>
        <ul>
          <li>Altura: <%= species.characteristics.altura %></li>
          <li>Diâmetro: <%= species.characteristics.diâmetro %></li>
          <li>Folhagem: <%= species.characteristics.folhagem %></li>
        </ul>
        <p class="card-text">Habitat: <%= species.habitat %></p>
      </div>
    </div>
    <a href="/species" class="btn btn-primary mt-3">Voltar para a lista</a>
  </body>
</html>
