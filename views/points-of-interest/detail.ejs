<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalhes do Ponto de Interesse</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="container mt-5">
    <h1 class="mb-4">Detalhes do Ponto de Interesse</h1>
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= point.name %></h5>
        <p>Latitude: <%= point.latitude %></p>
        <p>Longitude: <%= point.longitude %></p>
        <p>Descrição: <%= point.description %></p>
        <% if (point.qrCodeData) { %>
        <div>
          <img src="<%= point.qrCodeData %>" alt="QR Code" class="img-fluid" />
        </div>
        <% } %>
      </div>
    </div>
    <a href="/points-of-interest" class="btn btn-primary mt-3">Voltar</a>
  </body>
</html>
