<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Ponto de Interesse</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5;
        padding-top: 50px;
      }
      .container {
        max-width: 800px;
        margin: auto;
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      }
      .button-group {
        display: flex;
        justify-content: center; /* Centra os botões */
        gap: 1rem; /* Espaçamento entre os botões */
      }
      .icon-field {
        position: relative;
        display: flex;
        align-items: center;
      }
      .icon-field i {
        margin-right: 10px;
      }
      .icon-green {
        color: #006400;
      }

      .form-label {
        display: block;
        margin-top: 1rem;
      }
      .form-control {
        margin-top: 0.5rem;
      }
      h1 {
        color: #006400;
        margin-top: 1rem;
        margin-bottom: 1rem;
        text-align: center;
      }
      .btn-cancelar {
        margin-top: 1rem;
        background-color: #6c757d; /* Cor cinza para botão cancelar */
        color: white;
      }
      .btn-atualizar {
        margin-top: 1rem;
        background-color: #198754; /* Cor verde para botão atualizar */
        color: white;
      }
      .text-area-container {
        display: flex;
        flex-direction: column;
      }
      textarea {
        text-align: start; /* Alinha o texto para o inicio */
      }
    </style>
  </head>
  <body class="container mt-5">
    <h1 class="text-center mb-4">
      <i class="fa-solid fa-edit"></i> Editar Ponto de Interesse
    </h1>
    <form
      action="/points-of-interest/<%= point._id %>?_method=PUT"
      method="POST"
    >
      <div class="mb-3 icon-field">
        <i class="fa-solid fa-route icon-green"></i>
        <label for="name" class="form-label">Nome:</label>
        <input
          type="text"
          class="form-control text-start"
          id="name"
          name="name"
          value="<%= point.name %>"
          required
        />
      </div>
      <div class="mb-3 icon-field">
        <i class="fas fa-map-marked-alt icon-green"></i>
        <label for="latitude" class="form-label">Latitude:</label>
        <input
          type="text"
          class="form-control text-start"
          id="latitude"
          name="latitude"
          value="<%= point.latitude %>"
          required
        />
      </div>
      <div class="mb-3 icon-field">
        <i class="fas fa-map-marked icon-green"></i>
        <label for="longitude" class="form-label">Longitude:</label>
        <input
          type="text"
          class="form-control text-start"
          id="longitude"
          name="longitude"
          value="<%= point.longitude %>"
          required
        />
      </div>
      <div class="mb-3 text-area-container">
        <label for="description" class="form-label">
          <i class="fa-solid fa-align-left icon-green"></i> Descrição:
        </label>
        <textarea
          class="form-control text-start"
          id="description"
          name="description"
          rows="3"
        >
<%= point.description %></textarea
        >
      </div>
      <div class="mb-3 text-area-container">
        <label for="additionalInfo" class="form-label">
          <i class="fa-solid fa-info icon-green"></i> Informação Adicional:
        </label>
        <textarea
          class="form-control text-start"
          id="additionalInfo"
          name="additionalInfo"
          rows="3"
        >
<%= point.additionalInfo %></textarea
        >
      </div>
      <div class="button-group">
        <button type="submit" class="btn btn-atualizar">
          <i class="fa-solid fa-check"></i> Atualizar
        </button>
        <button
          type="button"
          class="btn btn-cancelar"
          onclick="window.history.back();"
        >
          <i class="fa-solid fa-arrow-left"></i> Cancelar
        </button>
      </div>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
