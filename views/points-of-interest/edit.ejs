<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Ponto de Interesse</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      .form-control:focus {
        box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
      }
      .form-label {
        font-weight: 500;
      }
      .container {
        max-width: 800px;
      }
      .icon-field {
        position: relative;
      }
      .icon-field i {
        position: absolute;
        right: 20px;
        top: 38px;
        color: #495057;
      }
      h1 {
        color: #006400; /* Cor que combina com a homepage */
      }
      .btn-success {
        margin-top: 1rem; /* Espaçamento antes do botão para melhor separação */
        width: 100%; /* Faz o botão ocupar toda a largura para uma ação clara */
      }
    </style>
  </head>
  <body class="container mt-5">
    <h1 class="text-center mb-4">Editar Ponto de Interesse</h1>
    <form
      action="/points-of-interest/<%= point._id %>?_method=PUT"
      method="POST"
    >
      <div class="mb-3 icon-field">
        <label for="name" class="form-label">Nome</label>
        <i class="fa-solid fa-route"></i>
        <input
          type="text"
          class="form-control"
          id="name"
          name="name"
          value="<%= point.name %>"
          required
        />
      </div>
      <div class="mb-3 icon-field">
        <label for="latitude" class="form-label">Latitude</label>
        <i class="fas fa-map-marked-alt"></i>
        <input
          type="text"
          class="form-control"
          id="latitude"
          name="latitude"
          value="<%= point.latitude %>"
          required
        />
      </div>
      <div class="mb-3 icon-field">
        <label for="longitude" class="form-label">Longitude</label>
        <i class="fas fa-map-marked"></i>
        <input
          type="text"
          class="form-control"
          id="longitude"
          name="longitude"
          value="<%= point.longitude %>"
          required
        />
      </div>
      <div class="mb-3">
        <label for="description" class="form-label">Descrição</label>
        <textarea
          class="form-control"
          id="description"
          name="description"
          rows="3"
        >
        <%= point.description %></textarea
        >
      </div>
      <div class="mb-3">
        <label for="additionalInfo" class="form-label"
          >Informações Adicionais</label
        >
        <textarea
          class="form-control"
          id="additionalInfo"
          name="additionalInfo"
          rows="3"
        >
        <%= point.additionalInfo %></textarea
        >
      </div>
      <button type="submit" class="btn btn-success">Atualizar</button>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
