<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Registro</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5; /* Cor de fundo mais suave */
      }

      .register-container {
        max-width: 800px;
        margin: 80px auto;
        padding: 25px;
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1); /* Sombra para profundidade */
      }

      .register-container h2 {
        text-align: center;
        color: #495057; /* Cor escura para melhor contraste */
        margin-bottom: 30px;
      }

      .btn-action {
        background-color: #28a745; /* Cor verde do botão */
        border: none;
        padding: 10px 20px; /* Padding ajustado */
        border-radius: 5px; /* Bordas arredondadas no botão */
        margin: 0 10px; /* Espaço entre os botões */
        width: auto; /* Largura automática para se ajustar ao conteúdo */
      }

      .btn-action:hover {
        background-color: #218838;
      }

      .btn-cancel {
        background-color: #6c757d; /* Cor para o botão cancelar */
      }

      .btn-cancel:hover {
        background-color: #5a6268;
      }

      .form-label {
        color: #6c757d; /* Cor suave para o texto dos rótulos */
      }

      .form-control {
        text-align: left; /* Alinha o texto dos campos à esquerda */
        border-radius: 5px; /* Bordas arredondadas nos campos de entrada */
        border: 1px solid #ced4da;
      }

      .button-group {
        display: flex;
        justify-content: center; /* Centraliza os botões */
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="register-container">
      <h2><i class="fas fa-user-plus"></i> Registo</h2>
      <form action="/users/register" method="POST">
        <div class="mb-3">
          <label for="username" class="form-label">Utilizador</label>
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            required
          />
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Senha</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="email"
            name="email"
            required
          />
        </div>
        <div class="mb-3">
          <label for="firstname" class="form-label">Nome</label>
          <input
            type="text"
            class="form-control"
            id="firstname"
            name="firstname"
            required
          />
        </div>
        <div class="mb-3">
          <label for="lastname" class="form-label">Sobrenome</label>
          <input
            type="text"
            class="form-control"
            id="lastname"
            name="lastname"
            required
          />
        </div>
        <div class="mb-3">
          <label for="phone" class="form-label">Telefone</label>
          <input type="text" class="form-control" id="phone" name="phone" />
        </div>
        <div class="button-group">
          <button type="submit" class="btn btn-action btn-success">
            Registar
          </button>
          <button
            type="button"
            class="btn btn-action btn-cancel"
            onclick="location.href='/'"
          >
            Cancelar
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
