<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Registro</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5; /* Cor de fundo mais suave */
      }

      .register-container {
        max-width: 400px;
        margin: 80px auto;
        padding: 25px;
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1); /* Sombra para profundidade */
      }

      .register-container h2 {
        text-align: center;
        color: #495057; /* Cor escura para melhor contraste */
        margin-bottom: 30px;
      }

      .btn-primary {
        width: 100%; /* Botão ocupando todo o espaço horizontal */
        background-color: #007bff;
        border: none;
        padding: 10px;
        border-radius: 5px; /* Bordas arredondadas no botão */
      }

      .btn-primary:hover {
        background-color: #0056b3;
      }

      .form-label {
        color: #6c757d; /* Cor suave para o texto dos rótulos */
      }

      .form-control {
        border-radius: 5px; /* Bordas arredondadas nos campos de entrada */
        border: 1px solid #ced4da;
      }
    </style>
  </head>
  <body>
    <div class="register-container">
      <h2>Registo</h2>
      <form action="/users/register" method="POST">
        <div class="mb-3">
          <label for="username" class="form-label">Utilizador</label>
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            required
          />
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Senha</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
          <!-- Complete com mais campos: Nome, Telefone, email, Endereço -->
        </div>
        <button type="submit" class="btn btn-success">Registar</button>
      </form>
    </div>
  </body>
</html>
