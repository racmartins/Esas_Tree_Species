<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        background-color: #e9f5f1; /* Um verde muito claro para o fundo */
      }

      .login-container {
        max-width: 400px;
        margin: 100px auto;
        padding: 20px;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra suave para a profundidade */
      }

      .login-container h2 {
        text-align: center;
        margin-bottom: 20px;
        color: #006400; /* Verde escuro para o título */
      }

      .btn-primary {
        background-color: #006400; /* Verde escuro */
        border: none;
      }

      .form-label {
        margin-bottom: 5px;
        color: #4d4d4d; /* Cinza escuro para os rótulos */
      }

      .form-control {
        border-radius: 5px;
        border: 1px solid #ced4da; /* Mantendo a borda padrão do Bootstrap */
      }

      .btn-primary:hover {
        background-color: #004d40; /* Um verde ainda mais escuro para o hover */
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <h2><i class="fas fa-door-open"></i> Login</h2>
      <form action="/users/login" method="POST">
        <div class="mb-3">
          <i class="fa-solid fa-user"></i>
          <label for="username" class="form-label">Utilizador</label>
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            required
          />
        </div>
        <div class="mb-3">
          <i class="fa-solid fa-lock"></i>
          <label for="password" class="form-label">Senha</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
        </div>
        <div class="d-flex justify-content-center">
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-sign-in-alt"></i> Entrar
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
