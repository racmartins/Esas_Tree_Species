<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Registo</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f0f2f5;
      }

      .register-container {
        max-width: 800px; /* Ajustado para um tamanho mais adequado */
        margin: 80px auto;
        padding: 25px;
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
      }

      .register-container h2 {
        text-align: center;
        color: #495057;
        margin-bottom: 30px;
      }

      .btn-register {
        width: 100%; /* Ajustado para que ocupe a largura total dentro do formulário */
        background-color: #28a745;
        border: none;
        padding: 10px;
        border-radius: 5px;
        margin-top: 20px;
        margin-bottom: 20px;
      }

      .btn-register:hover {
        background-color: #218838;
      }

      .form-label {
        color: #6c757d;
      }

      .form-control {
        text-align: left; /* Alinha o texto dos campos à esquerda */
        border-radius: 5px;
        border: 1px solid #ced4da;
        padding-left: 12px; /* Espaçamento interno para alinhar o texto */
      }

      /* Alinhamento dos campos e botão */
      .form-register .form-control {
        margin-bottom: 15px; /* Espaçamento entre os campos */
      }

      /* Centrar o botão dentro do formulário */
      .form-register .btn-register {
        margin-left: auto;
        margin-right: auto;
      }
    </style>
  </head>
  <body>
    <div class="register-container">
      <h2><i class="fas fa-user-plus"></i> Registo</h2>
      <form action="/users/register" method="POST" class="form-register">
        <div class="mb-3">
          <label for="username" class="form-label">Utilizador</label>
          <input
            type="text"
            class="form-control"
            id="username"
            name="username"
            required
          />
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Senha</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            required
          />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="email"
            name="email"
            required
          />
        </div>
        <div class="mb-3">
          <label for="firstname" class="form-label">Nome</label>
          <input
            type="text"
            class="form-control"
            id="firstname"
            name="firstname"
            required
          />
        </div>
        <div class="mb-3">
          <label for="lastname" class="form-label">Sobrenome</label>
          <input
            type="text"
            class="form-control"
            id="lastname"
            name="lastname"
            required
          />
        </div>
        <div class="mb-3">
          <label for="phone" class="form-label">Telefone</label>
          <input type="text" class="form-control" id="phone" name="phone" />
        </div>
        <!-- Botão centrado dentro do form -->
        <div class="text-center">
          <button type="submit" class="btn btn-success btn-register">
            Registar
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
