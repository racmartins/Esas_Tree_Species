<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Editar Jardim</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
    />
    <style>
      body {
        background-color: #f0f2f5; /* Um fundo suave para a página */
      }
      .container {
        max-width: 800px; /* Limita a largura do formulário para melhor leitura */
        margin: 50px auto; /* Centraliza e adiciona um espaçamento no topo */
        padding: 20px; /* Espaçamento interno para não colar no bordo */
        background-color: #fff; /* Fundo branco para a moldura */
        border-radius: 10px; /* Bordas arredondadas */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Sombra suave */
      }
      .icon-green {
        color: #006400;
      }
      .btn-cancelar {
        margin-top: 1rem;
        background-color: #6c757d; /* Cor cinza para botão cancelar */
        color: white;
      }
      .btn-atualizar {
        margin-top: 1rem;
        background-color: #198754; /* Cor verde para botão atualizar */
        color: white;
      }

      .btn {
        padding: 0.375rem 0.75rem; /* Ajusta o padding para tornar os botões menores */
      }
      .form-label {
        margin-top: 1rem; /* Espaçamento superior para os labels */
        display: block; /* Assegura que a label ocupa a própria linha */
      }

      .form-control {
        margin-top: 0.5rem;
      }
      .button-group {
        display: flex;
        justify-content: center; /* Centra os botões */
        gap: 1rem; /* Espaçamento entre os botões */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-5 text-center icon-green">
        <i class="fa fa-edit"></i> Editar Jardim
      </h1>
      <form action="/gardens/<%= garden._id %>/edit" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label"
            ><i class="fas fa-signature icon-green"></i> Nome do Jardim:</label
          >
          <input
            type="text"
            class="form-control"
            id="name"
            name="name"
            value="<%= garden.name %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="longitude" class="form-label"
            ><i class="fas fa-long-arrow-alt-right icon-green"></i>
            Longitude:</label
          >
          <input
            type="text"
            class="form-control"
            id="longitude"
            name="longitude"
            value="<%= garden.location.coordinates[0] %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="latitude" class="form-label"
            ><i class="fas fa-long-arrow-alt-up icon-green"></i>
            Latitude:</label
          >
          <input
            type="text"
            class="form-control"
            id="latitude"
            name="latitude"
            value="<%= garden.location.coordinates[1] %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="panoramicImage" class="form-label"
            ><i class="fas fa-image icon-green"></i> Imagem Panorâmica:</label
          >
          <input
            type="text"
            class="form-control"
            id="panoramicImage"
            name="panoramicImage"
            value="<%= garden.panoramicImage %>"
            placeholder="Caminho da imagem panorâmica"
            required
          />
        </div>
        <div class="button-group">
          <button type="submit" class="btn btn-atualizar">
            <i class="fas fa-save"></i> Atualizar Jardim
          </button>
          <button
            type="button"
            class="btn btn-cancelar"
            onclick="window.history.back();"
          >
            <i class="fas fa-arrow-left"></i> Cancelar
          </button>
        </div>
      </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
