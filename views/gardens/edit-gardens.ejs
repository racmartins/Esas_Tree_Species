<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Editar Jardim</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css"
    />
    <style>
      .container {
        padding-top: 40px;
      }
      .form-control:focus {
        box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
      }
      .btn-success {
        width: 100%;
      }
    </style>
  </head>
  <body>
    <div class="container shadow-sm p-3 mb-5 bg-body rounded">
      <h3 class="text-center mb-4" style="color: #006400">Editar Jardim</h3>
      <form action="/gardens/<%= garden._id %>/edit" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label"
            ><i class="bi bi-flower1"></i> Nome do Jardim:</label
          >
          <input
            type="text"
            class="form-control"
            id="name"
            name="name"
            value="<%= garden.name %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="longitude" class="form-label"
            ><i class="bi bi-geo-alt-fill"></i> Longitude:</label
          >
          <input
            type="text"
            class="form-control"
            id="longitude"
            name="longitude"
            value="<%= garden.location.coordinates[0] %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="latitude" class="form-label"
            ><i class="bi bi-geo-fill"></i> Latitude:</label
          >
          <input
            type="text"
            class="form-control"
            id="latitude"
            name="latitude"
            value="<%= garden.location.coordinates[1] %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="panoramicImage" class="form-label">
            <i class="fa fa-compass" aria-hidden="true"></i> Imagem Panorâmica
          </label>
          <input
            type="text"
            class="form-control"
            id="panoramicImage"
            name="panoramicImage"
            placeholder="Caminho da imagem panorâmica"
            required
            value="<%= garden.panoramicImage %>"
          />
        </div>
        <button type="submit" class="btn btn-success">Atualizar Jardim</button>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
